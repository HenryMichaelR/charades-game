name: Remove gesture recording

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/remove-motion-recording.yml
      - .github/scripts/remove_motion_recording.py

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Replace calibration with fixed tilt controls
        run: python .github/scripts/remove_motion_recording.py

      - name: Validate JavaScript
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          text = Path('index.html').read_text(encoding='utf-8')
          script = text.split('<script>', 1)[1].split('</script>', 1)[0]
          Path('/tmp/charades.js').write_text(script, encoding='utf-8')
          PY
          node --check /tmp/charades.js

      - name: Commit fixed tilt controls
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/scripts/remove_motion_recording.py
          git rm .github/workflows/remove-motion-recording.yml
          git add index.html
          git commit -m "Use fixed down-correct and up-skip controls"
          git push
